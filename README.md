# Агат

Чёрный кот с золотыми глазами. Спроси — ответит.

**Пакет:** `com.thekingoffamily.agat`  
**Магазин:** RuStore · бесплатно · 0+

Один экран. Без аккаунта. Баннер РСЯ (пока демо `demo-banner-yandex`).

```
cd app
flutter pub get
flutter test
flutter build apk --release
```

Иконка: `branding/logo-512.png` (512×512, без прозрачности).
